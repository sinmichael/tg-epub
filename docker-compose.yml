services:
  tg-epub:
    build: .
    container_name: tg-epub
    restart: unless-stopped
    env_file: .env
    volumes:
      - tg-epub-data:/app/data
    environment:
      - DATA_DIR=/app/data

volumes:
  tg-epub-data:
